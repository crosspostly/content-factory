# 🚀 Auto-Fix System - Quick Start

## Что это?

**Auto-Fix System** - это полностью автоматическая система, которая:
1. ✅ Автоматически исправляет простые ошибки тестов (60-70% случаев)
2. ✅ Создает детальные ТЗ для сложных ошибок (30-40% случаев)
3. ✅ Гарантирует что `main` НИКОГДА не ломается

## 🎯 За 30 секунд

```bash
# 1. Ты пушишь код
git push origin feature/my-feature

# 2. Если тесты ❌:
#    - Auto-Fix анализирует ошибку
#    - ПРОСТАЯ? → Сам исправляет → Создает PR → Auto-merge ✅
#    - СЛОЖНАЯ? → Создает ТЗ → Ты правишь → Создаешь PR → Merge ✅

# 3. main всегда ✅
```

## 📋 Что автоматически исправляется?

### SIMPLE (AUTO-FIX) 🤖
- ❌ `ImportError` → ✅ Добавить import
- ❌ `ModuleNotFoundError` → ✅ Добавить в requirements.txt
- ❌ `SyntaxError` → ✅ Запустить black
- ❌ `AttributeError` (простые) → ✅ Добавить метод
- ❌ `FileNotFoundError` → ✅ Создать файл
- ❌ YAML syntax → ✅ Исправить синтаксис

**Время исправления:** ~3-5 минут

### COMPLEX (MANUAL) 👤
- 🧠 Logic bugs (нужна бизнес-логика)
- 🏗️ Architecture issues
- 🌐 API integration
- ⚡ Performance
- ⚙️ Configuration

**Что происходит:** Создается Issue с детальным ТЗ → Ты исправляешь

## 🔧 Настройка (1 раз)

### 1. GitHub Secrets
```
Settings → Secrets and variables → Actions → New repository secret
```

**Required:**
- `GOOGLE_AI_API_KEY` - Gemini API key (для анализа)

**Optional:**
- `OPENROUTER_API_KEY` - Qwen API key (альтернатива Gemini)

### 2. Готово!
Больше ничего настраивать не нужно. Все workflows уже настроены.

## 📊 Как это работает?

```
╔════════════════════════════════════════════════════════════════╗
║                    git push origin feature/X                   ║
╚════════════════════════════════════════════════════════════════╝
                              ↓
                   GitHub Actions: tests.yml
                              ↓
                  ┌───────────┴───────────┐
                  │                       │
              ✅ Passed              ❌ Failed
                  │                       │
              🎉 Done!         auto-fix-agent.yml
                                          ↓
                              Qwen/Gemini анализирует
                                          ↓
                           Создает GitHub Issue
                                          ↓
                      ┌───────────────────┴────────────────┐
                      │                                    │
                  SIMPLE                              COMPLEX
         (auto-fix-ready label)              (needs-dev-task label)
                      │                                    │
              auto-fix.yml                        create-task.yml
                      │                                    │
          Генерирует fix через LLM               Создает детальное ТЗ
                      │                                    │
          Создает branch + PR                     Пингует разработчика
                      │                                    │
              Тесты запускаются                   Ждет manual PR
                      │                                    │
                  ✅ Pass?                            Ты исправляешь
                      │                                    │
                  auto-merge.yml                       Manual merge
                      │                                    │
              Мёржит в main ✅                        Main updated ✅
╚═══════════════════════════════════════════════════════════════╝
```

## 📝 Примеры

### Пример 1: ImportError (авто-исправление)

**Твой код:**
```python
# Забыл import
config = ProjectConfig.load('config.yaml')  # ❌ NameError
```

**Auto-Fix делает:**
1. Анализирует: "Missing import for ProjectConfig"
2. Генерирует fix: `from core.utils.config_loader import ProjectConfig`
3. Создает PR с исправлением
4. Тесты проходят ✅
5. Auto-merge в main
6. **Время:** 3-5 минут

**Ты:** Ничего не делаешь, просто получаешь уведомление что исправлено ✅

### Пример 2: Logic Bug (ручное исправление)

**Твой код:**
```python
def calculate_score(base):
    return base  # ❌ Wrong! Should be base * 2
```

**Auto-Fix делает:**
1. Анализирует: "Test expects 100 but got 50 - logic error"
2. Создает Issue с ТЗ:
   ```
   ## Проблема
   calculate_score() возвращает неправильное значение
   
   ## Файл
   core/utils/scoring.py, line 15
   
   ## Что нужно
   Функция должна возвращать base * 2, а не просто base
   
   ## Шаги
   1. Открыть core/utils/scoring.py
   2. Найти функцию calculate_score()
   3. Изменить return base на return base * 2
   4. Запустить pytest tests/test_scoring.py
   5. Создать PR
   ```

**Ты:**
1. Читаешь Issue
2. Исправляешь код
3. Создаешь PR: "Fixes #123"
4. Мёржишь после review

## 🎨 Метки Issues

- `auto-fix-ready` 🤖 - Будет исправлено автоматически
- `needs-dev-task` 👤 - Нужна ручная работа
- `auto-generated` - Создано Auto-Fix Agent
- `ai-analyzed` - Проанализировано AI
- `bug` - Это баг

## 🚫 Ограничения

Auto-Fix **НЕ**:
- ❌ Не мёржит в `main` напрямую (только через PR)
- ❌ Не запускается на `main` (только на feature branches)
- ❌ Не делает auto-merge если тесты падают
- ❌ Не удаляет код (только добавляет/изменяет)
- ❌ Не исправляет без создания Issue (для прозрачности)

## 📞 Поддержка

### Auto-Fix не сработал?

1. **Проверь GitHub Actions** - открой workflow logs
2. **Проверь метки Issue** - должна быть `auto-fix-ready` или `needs-dev-task`
3. **Проверь что ветка не `main`** - на main Auto-Fix не работает
4. **Проверь секреты** - `GOOGLE_AI_API_KEY` должен быть настроен

### Auto-Fix создал неправильный PR?

1. Закрой PR (не мёржь!)
2. Оставь комментарий в Issue что пошло не так
3. Удали метку `auto-fix-ready`
4. Добавь метку `needs-dev-task`
5. Исправь вручную

### Хочу отключить Auto-Fix?

Удали метку `auto-fix-ready` из Issue. Workflow не запустится.

## 🎉 Результат

**main НИКОГДА не ломается!** ✅

Все ошибки либо исправляются за 3-5 минут, либо превращаются в четкие задачи.

## 📚 Подробная документация

- [AUTO_FIX_SYSTEM.md](./AUTO_FIX_SYSTEM.md) - Полная документация
- [WORKFLOWS.md](./WORKFLOWS.md) - Все workflows
- [core/auto_fix_agent.py](./core/auto_fix_agent.py) - Исходный код

---

*Система создана для Content Factory - AI Video Generation Platform*
