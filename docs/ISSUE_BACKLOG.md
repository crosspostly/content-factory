# ISSUE_BACKLOG — будущие GitHub Issues (Phase 3)

**Дата обновления:** 2025-12-17  
**Источник:** подтверждённые проблемы в коде/пайплайнах (см. также: [`docs/DEVELOPMENT_ROADMAP.md`](./DEVELOPMENT_ROADMAP.md), [`docs/WORKFLOW_AUDIT.md`](./WORKFLOW_AUDIT.md), [`docs/ISSUE_43_IMPLEMENTATION.md`](./ISSUE_43_IMPLEMENTATION.md)).

## Обозначения

- **Priority:** P0 (блокер) → P1 (важно) → P2 (улучшение/инфраструктура)
- **Severity:** `critical` / `high` / `medium` / `low`

## Краткая таблица (для планирования)

| # | Priority | Severity | Title | Область | Ключевые файлы |
|---:|:--:|:--:|---|---|---|
| 1 | P0 | critical | Silent TTS: генерируются «немые» WAV (Gemini вызывается без audio_config) | core pipeline | `core/generators/tts_generator.py` |
| 2 | P1 | high | Pixabay background video не работает: `_get_pixabay_video` незавершён и не используется → всегда градиент | renderer | `core/generators/video_renderer.py` |
| 3 | P1 | medium | Несогласованные значения `--mode` между workflow и CLI (регресс `long-form` vs `long_form`) | tooling | `.github/workflows/generate-batch.yml`, `core/orchestrators/pipeline_orchestrator.py` |
| 4 | P1 | high | Slides mode registry нигде не вызывается → Issue #43 фактически не доставлен в основной pipeline | modes | `core/content_modes/*`, `core/orchestrators/pipeline_orchestrator.py` |
| 5 | P1 | high | Subtitles + Upload: отсутствует генерация субтитров и uploader (stub/NotImplemented) | pipeline features | `core/generators/video_renderer.py`, `core/uploaders/youtube_uploader.py` |
| 6 | P2 | medium | AI Code Review workflow нерабочий без настроенного auth/secrets (OIDC) — донастроить или удалить | workflows | `.github/workflows/ai-code-review.yml` |
| 7 | P2 | medium | Auto-fix workflow создаёт шум: открывает issues даже без API key / без реальных авто-фиксов | workflows | `.github/workflows/auto-fix-failures.yml` |

---

## 1) Silent TTS: генерируются «немые» WAV (Gemini вызывается без audio_config)

- **Title:** Silent TTS: `tts_generator` создаёт silent WAV вместо реального голоса
- **Priority:** P0
- **Severity:** critical

### Где в коде

- `core/generators/tts_generator.py`
  - `_synthesize_gemini_tts_async(...)` — вызов `client.models.generate_content(...)` без конфигурации аудио (примерно строки **100–150**, особенно **125–149**).
  - Фолбек `_create_silent_wav(...)` используется как «нормальный» путь выполнения.

### Как воспроизвести

1. Запустить pipeline (или напрямую TTS) с реальным `GOOGLE_AI_API_KEY`:
   - `python -m core.orchestrators.pipeline_orchestrator --project youtube_horoscope --mode shorts`
2. Открыть файл `output/audio/<project>/shorts_main.wav`.
3. Проверить, что звук отсутствует (тишина), хотя длительность корректная.

### Ожидаемый результат

- При валидном API ключе и доступном TTS-бэкенде должен генерироваться **реальный** аудиосигнал.
- Silent WAV допускается только как **последний** фолбек при ошибках, а не как happy-path.

### ТЗ / план работ

- Перевести вызов Gemini на корректный режим вывода аудио (modality/`audio_config`/конфиг генерации в `google-genai` SDK).
- Явно валидировать, что ответ содержит аудио-байты и формат известен (mp3/wav).
- Улучшить логику фолбеков:
  - 1) retry с безопасными параметрами,
  - 2) понятная ошибка,
  - 3) silent WAV только когда надо «не уронить» renderer.
- Добавить тест/проверку, что сгенерированный WAV **не полностью нулевой** (например, RMS/peak threshold) при mock/stub аудио.

### Критерии приёмки (Acceptance Criteria)

- [ ] При валидном API ключе `tts_generator.synthesize(...)` возвращает аудио, которое **не является полной тишиной**.
- [ ] Silent WAV не используется на happy-path (только при исключениях/недоступности TTS).
- [ ] Добавлены/обновлены тесты для поведения TTS (минимум: проверка, что фолбек включается только при ошибке).
- [ ] Smoke-run orchestrator генерирует MP4 с слышимой дорожкой.

---

## 2) Pixabay background video не работает: `_get_pixabay_video` незавершён и не используется → всегда градиент

- **Title:** Реализовать и подключить Pixabay video background (с корректным fallback)
- **Priority:** P1
- **Severity:** high

### Где в коде

- `core/generators/video_renderer.py`
  - `_get_pixabay_video(...)` содержит `pass` и логически противоречивый разбор ответа (примерно строки **62–132**).
  - В рендерерах (`_render_shorts/_render_long_form/_render_ad`) фон всегда создаётся через `_create_background_clip(...)` (градиент), загрузка/использование Pixabay видео не выполняется.

### Как воспроизвести

1. Установить `PIXABAY_API_KEY`.
2. Запустить генерацию видео (shorts/long_form/ad).
3. Убедиться, что фон всегда «градиентный», а не реальное видео.

### Ожидаемый результат

- При наличии `PIXABAY_API_KEY` и успешном ответе API — фон берётся из Pixabay (видео скачивается и подставляется как background).
- При отсутствии ключа/ошибке API — чистый fallback на `_create_background_clip(...)` без падений.

### ТЗ / план работ

- Завершить `_get_pixabay_video(...)`:
  - корректно обрабатывать структуру `data["hits"][i]["videos"]` и выбирать подходящее качество;
  - корректно учитывать `duration_sec` (min/max duration);
  - возвращать **прямой URL** на видео-файл.
- Подключить Pixabay-background в `_render_shorts/_render_long_form/_render_ad`:
  - скачивание в `output/assets/pixabay_cache/...` (кэш),
  - `VideoFileClip` как фон, crop/resize под target aspect.
- Добавить unit-тесты с mock `requests.get` (без реальных сетевых вызовов).

### Критерии приёмки (Acceptance Criteria)

- [ ] При корректном `PIXABAY_API_KEY` рендер использует реальный видеоряд как фон (хотя бы для режима `shorts`).
- [ ] При отсутствии ключа фон остаётся градиентным, pipeline не падает.
- [ ] `_get_pixabay_video(...)` не содержит `pass`, покрыт тестами (mock).

---

## 3) Несогласованные значения `--mode` между workflow и CLI (регресс `long-form` vs `long_form`)

- **Title:** Защититься от рассинхронизации значений `--mode` между GitHub Actions и CLI
- **Priority:** P1
- **Severity:** medium

### Где в коде / конфиге

- CLI выбор режима:
  - `core/orchestrators/pipeline_orchestrator.py` → `build_parser(..., choices=["shorts", "long_form", "ad"])`
- Workflow input:
  - `.github/workflows/generate-batch.yml` → `inputs.format.options` и далее `--mode "$FORMAT"`

### Как воспроизвести

- При любом рассинхроне (например, если в workflow/доках указать `long-form` вместо `long_form`) запуск batch/CLI ломается:
  - `python -m core.generators.batch_generator ... --mode long-form` → ошибка валидации аргументов.

### Ожидаемый результат

- Workflow inputs, документация и CLI используют **одинаковый** набор значений.
- При ошибочном значении ошибка должна быть понятной и обнаруживаться **раньше** (в CI/линте), а не при ручном запуске.

### ТЗ / план работ

- Добавить автоматическую проверку (тест или небольшой скрипт), что:
  - `generate-batch.yml` `inputs.format.options` ⊆ `pipeline_orchestrator.build_parser()._option_string_actions["--mode"].choices` (или эквивалент).
- (Опционально) вынести список допустимых mode’ов в единый модуль/константу и переиспользовать.

### Критерии приёмки (Acceptance Criteria)

- [ ] В репозитории есть тест/скрипт, который падает, если workflow предлагает невалидный `--mode`.
- [ ] В README/доках для batch-generation перечислены корректные значения (`shorts`, `long_form`, `ad`).

---

## 4) Slides mode registry нигде не вызывается → Issue #43 фактически не доставлен в основной pipeline

- **Title:** Интегрировать Slides Mode в `pipeline_orchestrator` (доставить Issue #43 в pipeline)
- **Priority:** P1
- **Severity:** high

### Где в коде

- Реализация Slides mode есть:
  - `core/content_modes/slides_mode/*` (см. `docs/ISSUE_43_IMPLEMENTATION.md`)
- Но orchestrator не умеет его запускать:
  - `core/orchestrators/pipeline_orchestrator.py` поддерживает только `shorts/long_form/ad`.

### Как воспроизвести

- Попытка запустить slides режим через основной CLI невозможна:
  - `python -m core.orchestrators.pipeline_orchestrator --project horoscope_leo --mode slides` → CLI отклонит значение.

### Ожидаемый результат

- Slides mode должен быть доступен как полноценный режим генерации (через CLI или через config), используя `ContentModeRegistry.get("slides", ...)`.

### ТЗ / план работ

- Определить интерфейс запуска:
  - вариант A: добавить `--mode slides` в orchestrator;
  - вариант B: добавить отдельный флаг `--video-mode slides` и не ломать существующий `--mode`.
- Подключить `core.content_modes` так, чтобы registry гарантированно регистрировал `slides` (см. рекомендации в `docs/ISSUE_43_IMPLEMENTATION.md`).
- Добавить интеграционный тест (mock TTS/renderer) для `slides` пути.

### Критерии приёмки (Acceptance Criteria)

- [ ] Можно сгенерировать видео в режиме `slides` через основной entrypoint (CLI/orchestrator).
- [ ] Есть тест, подтверждающий, что вызывается `ContentModeRegistry.get("slides")` и возвращается корректный `GenerationResult`.
- [ ] Документация обновлена: как запускать slides (пример команды/конфига).

---

## 5) Subtitles + Upload: отсутствует генерация субтитров и uploader (stub/NotImplemented)

- **Title:** Реализовать Subtitles MVP + привести upload-часть в рабочее/безопасное состояние
- **Priority:** P1
- **Severity:** high

### Где в коде

- Subtitles:
  - В репозитории нет модулей генерации/встраивания субтитров; `core/generators/video_renderer.py` не добавляет subtitles на видео.
- Upload:
  - `core/uploaders/youtube_uploader.py` — stub с `raise NotImplementedError(...)`.
  - `core/orchestrators/pipeline_orchestrator.py` при `--upload` вызывает этот uploader.

### Как воспроизвести

- Subtitles:
  - Сгенерировать любое видео → субтитров нет (burn-in / отдельной дорожки).
- Upload:
  - Запустить orchestrator с `--upload --platforms youtube` → падение на `NotImplementedError`.

### Ожидаемый результат

- Subtitles: минимальный рабочий режим (MVP) — субтитры появляются хотя бы в `shorts` (burn-in поверх видео).
- Upload: либо реализована реальная загрузка на YouTube (MVP), либо orchestrator **не падает**, а сообщает, что upload пока недоступен.

### ТЗ / план работ

- Subtitles MVP:
  - генерировать субтитры из текста сценария (без ASR),
  - распределять тайминги по длительности аудио (`audio_map["total_duration_sec"]`) и длине строк,
  - интеграция в `video_renderer` (burn-in) под флагом в конфиге.
- Upload:
  - вариант A (MVP): реализовать YouTube upload (OAuth / API client) и вернуть `video_id`;
  - вариант B (временно): сделать uploader «no-op» с понятным сообщением + отключить флаг/инструкции в README.
- Добавить тесты:
  - subtitles: что шаг включается при флаге и создаёт overlays,
  - upload: что orchestrator не падает (или что upload возвращает id при mock).

### Критерии приёмки (Acceptance Criteria)

- [ ] Есть конфиг-флаг для включения субтитров, и при включении субтитры визуально присутствуют на выходном MP4.
- [ ] `--upload --platforms youtube` больше не приводит к `NotImplementedError` (реальная загрузка или безопасный no-op).
- [ ] Добавлены тесты, покрывающие subtitles/upload сценарии.

---

## 6) AI Code Review workflow нерабочий без настроенного auth/secrets (OIDC) — донастроить или удалить

- **Title:** Починить или архивировать `.github/workflows/ai-code-review.yml`
- **Priority:** P2
- **Severity:** medium

### Где

- `.github/workflows/ai-code-review.yml`
  - PR-триггер отключён (закомментирован),
  - шаг OIDC auth помечен `if: false` и требует секреты (`GOOGLE_WORKLOAD_IDENTITY_PROVIDER`, `GOOGLE_SERVICE_ACCOUNT`).

### Как воспроизвести

- Попытка включить PR trigger без настройки secrets/auth приведёт к неработающему workflow.
- Ручной `workflow_dispatch` часто бесполезен без корректной аутентификации.

### Ожидаемый результат

- Либо workflow реально работает (автоматически/вручную) и оставляет review-комментарии,
- Либо он явно архивирован/удалён, чтобы не вводить в заблуждение и не создавать техдолг.

### ТЗ / план работ

- Вариант A (починить):
  - выбрать способ auth (OIDC или API key),
  - описать требуемые секреты в `docs/WORKFLOW_AUDIT.md`/README,
  - включить PR trigger.
- Вариант B (архивировать):
  - переименовать в `ai-code-review.yml.disabled` и удалить упоминания из документации.

### Критерии приёмки (Acceptance Criteria)

- [ ] Выбран один путь: «починить» или «архивировать».
- [ ] Если «починить»: есть рабочий запуск и понятная документация по секретам.
- [ ] Если «архивировать»: workflow не запускается и не фигурирует в README как рабочий.

---

## 7) Auto-fix workflow создаёт шум: открывает issues даже без API key / без реальных авто-фиксов

- **Title:** Уменьшить шум от `auto-fix-failures.yml`: не создавать issues без ключа, добавить дедупликацию
- **Priority:** P2
- **Severity:** medium

### Где

- `.github/workflows/auto-fix-failures.yml`
  - Job запускается на `workflow_run` при падении тестов.
  - Auto-fix loop выполняется только при `env.GEMINI_API_KEY != ''`, но шаг создания «замороженного issue» (`Create Frozen Issue`) выполняется при `success == false` — в том числе когда авто-фикс даже не запускался (нет ключа).

### Как воспроизвести

1. В репозитории без настроенного `GOOGLE_AI_API_KEY/GEMINI_API_KEY` добиться падения `tests.yml` на main/feature-ветке.
2. Дождаться запуска `Auto-Fix Test Failures`.
3. Наблюдать автоматическое создание issue `🧊 [AUTO-FIX FAILED] ...` даже при отсутствии ключа.

### Ожидаемый результат

- Если AI ключ не настроен — workflow должен завершаться тихо/нейтрально (без issue/PR), либо создавать один раз понятный «setup required».
- Если ключ настроен — workflow должен создавать PR только при успешном авто-фиксе, а при неуспехе:
  - дедуплицировать issues (не плодить каждый раз),
  - добавлять ссылку на run и branch/sha.

### ТЗ / план работ

- Добавить явный guard:
  - не создавать frozen issue, если `GEMINI_API_KEY` пустой (или если auto-fix step пропущен).
- Добавить дедупликацию:
  - искать существующий issue по branch+SHA / по заголовку и обновлять его комментарием вместо создания нового.
- (Опционально) добавить настройку: включать auto-fix только при label/opt-in.

### Критерии приёмки (Acceptance Criteria)

- [ ] При отсутствии AI ключа workflow **не создаёт** auto-fix issues.
- [ ] При наличии AI ключа workflow создаёт PR при успехе.
- [ ] При неуспехе создаётся максимум **один** issue на уникальную комбинацию (ветка+sha или run-id), последующие запуски обновляют существующий.
