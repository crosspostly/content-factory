# 🎬 Content Factory - AI-Powered Multi-Platform Content Machine

> **Фабрика контента, работающая на GitHub Actions. Генерирует видео и контент для YouTube, TikTok, Instagram, VK с поддержкой Gemini, Edge-TTS, стоками и масштабируемыми проектами.**

![Version](https://img.shields.io/badge/version-2.2-blue)
![Status](https://img.shields.io/badge/status-active-green)
![License](https://img.shields.io/badge/license-MIT-orange)
![Python](https://img.shields.io/badge/python-3.11%2B-blue)

---

## 🔄 Что Нового (v2.2)

- Добавлен **проект YouTube Гороскопы**: `projects/youtube_horoscope/`
- Единый конфиг голосов с поддержкой **Edge-TTS** и **Gemini TTS** (fallback + выбор по типу видео)
- Промпты для **шортов, длинных видео (20–45 мин, блоки любовь/деньги/здоровье)** и **рекламных вставок**
- Отдельный гайд: `HOROSCOPE_PROJECT_SETUP.md` с описанием всех необходимых **API ключей, GitHub Secrets и стоков**
- Закладка на мультиплатформенный upload (YouTube, TikTok, Instagram, VK) через конфиг

---

## 📁 Проект YouTube Гороскопы (пример проекта)

**Путь:** `projects/youtube_horoscope/`

### Основные файлы

- `config.yaml` — конфигурация проекта
- `prompts/shorts_scenario.txt` — генератор вирусных шортов (хуки, триггеры)
- `prompts/long_form_scenario.txt` — генератор длинных видео (20–45 мин, блоки по сферам)
- `prompts/ads_product_script.txt` — генератор нативной рекламы в гороскопном контенте
- `ads/` — папка под рекламируемые продукты и ассеты (будет расширена)
- `content_plan.json` — план контента (по датам/неделям)

### Особенности длинных видео

- Длительность: таргет **25–35 мин**, min 20, max 45
- Структура блоками:
  - Вступление + объяснение формата (смотреть свой и чужие знаки)
  - **LOVE блок**: все знаки, по 35–45 сек, фокус на отношениях
  - **MONEY блок**: все знаки, по 35–45 сек, деньги/работа/бизнес
  - **HEALTH/ENERGY блок**: по 20–30 сек на знак
  - Special section: совместимость/ритуал/удачные дни
  - Wrap-up: резюме + CTA + манифестации
- В промпте заложено:
  - прямая рекомендация слушать **знаки близких**
  - идея **манифестации**: слушаешь чужие хорошие прогнозы → "подтягиваешь" это в свою реальность
  - лайки/комменты подаются как "якорь"/акт манифестации

### Настройка голосов и TTS

В `config.yaml` проекта:

```yaml
audio:
  primary_engine: "edge-tts"
  fallback_engine: "gemini-tts"

  engines:
    edge_tts:
      enabled: true
      voices:
        female_neutral: "ru-RU-DariyaNeural"
        female_warm: "ru-RU-SvetlanaNeural"
    gemini_tts:
      enabled: true
      model: "gemini-2.5-flash"
      voices:
        female_neutral: "Gladia"
        female_warm: "Stella"
        female_mysterious: "Luna"

  voice_selection:
    shorts: "edge-tts"
    long_form: "gemini-tts"
    ads: "edge-tts"
```

Дальше Python‑агент читает этот конфиг и **сам выбирает движок и голос** по типу видеоролика, без хардкода.

---

## 🔐 GitHub Secrets и API Ключи

Подробный список для проекта гороскопов — в `HOROSCOPE_PROJECT_SETUP.md`. Кратко:

### Обязательные Secrets (минимум)

| Secret | Для чего |
|--------|---------|
| `YOUTUBE_API_KEY` | YouTube Data API (загрузка видео, плейлисты) |
| `GOOGLE_AI_API_KEY` | Gemini API (LLM + TTS, Vision) |
| `GEMINI_TTS_ENABLED` | Включение TTS от Gemini |
| `GEMINI_TTS_QUOTA_DAILY` | Лимит запросов в день (для graceful fallback) |
| `TELEGRAM_BOT_TOKEN` | Уведомления и управление через Telegram-бота |
| `TELEGRAM_CHAT_ID` | Куда слать уведомления |
| `EDGE_TTS_ENABLED` | Использовать Edge-TTS как fallback/основной движок |
| `GITHUB_TOKEN` | Взаимодействие с GitHub API из workflow/агентов |

При необходимости добавляются:

- ключи для TikTok/Instagram/VK upload
- ключи для Pexels/Pixabay, если используем их API напрямую

---

## 🎵 Стоки: Видео, Музыка, Эффекты

В `config.yaml` проекта гороскопов уже заданы рекомендуемые источники:

### Видео-фоны (stars, tarot, mystical)

- **Pexels** — `https://www.pexels.com/videos` (бесплатно, API с разумным rate limit)
- **Pixabay** — `https://pixabay.com/videos` (бесплатно, ~500 запросов/час на API)
- **Unsplash** — `https://unsplash.com` (главно фото, но хватает и для фонов)

### Музыка

- **Pixabay Music** — бесплатная музыка, без авторских отчислений при соблюдении условий
- **Bensound** — фри-треки с атрибуцией (нужно следить за лицензией)
- **Epidemic Sound** — платный, но качественный вариант (если будет подписка)

### Звуковые эффекты

- **Pixabay SFX** — бесплатные звуки
- **Freesound** — огромная база, нужно следить за лицензиями CC

Настройки в `config.yaml` позволяют агенту понимать **откуда брать**:

```yaml
audio:
  background_music:
    sources: ["epidemic_sound", "pixabay_music", "bensound"]
    preferred_source: "pixabay_music"

  sound_effects:
    sources: ["pixabay_sfx", "freesound"]
    preferred_source: "pixabay_sfx"
    effects:
      mystical_transition: "whoosh or magic sound"
      zodiac_sign_appear: "chime or bell sound"
      love_section: "heart beat or romantic tone"
      money_section: "coin sound or success tone"
      warning: "soft alert or shimmer"
      lucky_element: "sparkle or positive chime"
```

---

## 🌍 Платформы (YouTube, TikTok, Instagram, VK)

Текущая архитектура **изначально задумана как мультиплатформенная**:

- Каждое видео генерится в **мастер-формате**:
  - вертикалка (1080×1920) для шортов/рилс/тик-ток
  - горизонт (1920×1080) для классических YouTube-видео
- Далее upload‑слой (будет в `core/uploaders/`) может разложить контент по:
  - **YouTube** (Shorts + обычные видео)
  - **TikTok**
  - **Instagram Reels**
  - **VK Клипы / Видео**

Конкретные флаги платформ будут указываться в конфиге проекта, например:

```yaml
upload:
  platforms:
    youtube:
      enabled: true
    tiktok:
      enabled: true
    instagram:
      enabled: true
    vk:
      enabled: true
```

Агент читает этот список и для каждого сгенерённого видео:

1. Берёт подготовленный мастер-файл
2. При необходимости делает трансформации под требования платформы
3. Вызывает соответствующий uploader.

---

## 🧠 Универсальность и Оркестратор

Архитектура задумывается как **универсальная фабрика**, а не «скрипт под один канал»:

- Любой канал/проект = новая папка в `projects/` с:
  - `config.yaml`
  - набором `prompts/`
  - `ads/` и `content_plan.json`
- Весь общий код и оркестрация — в `core/`.

Планируемые ключевые модули:

```text
core/
  orchestrators/
    pipeline_orchestrator.py   # главный оркестратор пайплайна
  generators/
    script_generator.py        # вызывает LLM по промптам
    tts_generator.py           # работа с Edge/Gemini TTS
    video_renderer.py          # сборка видео из фонов/текста/звуков
  uploaders/
    youtube_uploader.py
    tiktok_uploader.py
    instagram_uploader.py
    vk_uploader.py
  utils/
    config_loader.py           # чтение config.yaml
    model_router.py            # выбор LLM (Gemini → Gemma → OpenRouter)
    tts_router.py              # выбор TTS-движка/голоса
    stock_client.py            # обёртки над Pexels/Pixabay/Unsplash
    secrets_manager.py         # доступ к GitHub Secrets/.env
```

На текущем шаге уже готовы конфиги и промпты, а **детальное техзадание для этих модулей** будет оформлено отдельно (как `TECH_SPEC_PIPELINE.md`), чтобы агенты/код могли этот план до конца реализовать.

---

Остальная часть README (описание кэширования, общая архитектура, установка) сохранена из предыдущей версии и дополнена этим блоком.
