# 🔴 DISABLED - Notifications workflow disabled
# Only auto-fix-failures.yml should create issues
# This prevents cascading workflow failures

name: Notifications & Alerts

on:
  workflow_run:
    workflows:
      - "Run Tests"
    types:
      - completed

jobs:
  disable-notification:
    runs-on: ubuntu-latest
    steps:
      - name: "ℹ️ Info"
        run: |
          echo "📵 Notifications workflow is DISABLED"
          echo "✅ Only auto-fix-failures.yml manages issues"
          echo "🤖 To re-enable: Update notifications.yml"
