name: Generate Horoscope Video

on:
  workflow_dispatch:
    inputs:
      format:
        description: 'Video format (shorts | long-form | ad)'
        required: true
        default: 'shorts'
        type: choice
        options:
          - shorts
          - long-form
          - ad
      date:
        description: 'Horoscope date (YYYY-MM-DD, optional - uses today)'
        required: false
        type: string
      project:
        description: 'Project name'
        required: false
        default: 'youtube_horoscope'
        type: string
  
  schedule:
    - cron: '0 6 * * *'  # Daily 06:00 UTC = 09:00 MSK

jobs:
  generate-video:
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      
      - name: Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg imagemagick ghostscript fonts-dejavu-core libsm6 libxext6
          echo "✅ System dependencies installed"
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -q
          echo "✅ Python dependencies installed"
      
      - name: Determine format and date
        id: params
        run: |
          FORMAT="${{ github.event.inputs.format || 'shorts' }}"
          DATE="${{ github.event.inputs.date }}"
          PROJECT="${{ github.event.inputs.project || 'youtube_horoscope' }}"
          
          if [ -z "$DATE" ]; then
            DATE=$(date +%Y-%m-%d)
          fi
          
          echo "format=$FORMAT" >> $GITHUB_OUTPUT
          echo "date=$DATE" >> $GITHUB_OUTPUT
          echo "project=$PROJECT" >> $GITHUB_OUTPUT
          
          echo "📅 Format: $FORMAT"
          echo "📆 Date: $DATE"
          echo "📦 Project: $PROJECT"
      
      - name: Generate Content (Script → TTS → Video)
        id: generate
        env:
          GOOGLE_AI_API_KEY: ${{ secrets.GOOGLE_AI_API_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
          FORMAT: ${{ steps.params.outputs.format }}
          DATE: ${{ steps.params.outputs.date }}
          PROJECT: ${{ steps.params.outputs.project }}
        run: |
          python3 << 'PYTHON_SCRIPT'
          import os
          import sys
          from pathlib import Path
          
          project = os.getenv('PROJECT')
          fmt = os.getenv('FORMAT')
          target_date = os.getenv('DATE')
          
          print(f"\n" + "="*70)
          print(f"🎬 HOROSCOPE VIDEO GENERATION")
          print(f"="*70)
          print(f"Project: {project}")
          print(f"Format: {fmt}")
          print(f"Date: {target_date}\n")
          
          # ========== PART 1: SCRIPT GENERATION =========="
          print(f"📝 PART 1: Script Generation")
          print(f"-" * 70)
          
          try:
              from core.generators import script_generator
              
              if fmt == "shorts":
                  script = script_generator.generate_short(target_date=target_date)
              elif fmt == "long_form":
                  script = script_generator.generate_long_form(target_date=target_date)
              elif fmt == "ad":
                  script = script_generator.generate_ad(target_date=target_date)
              else:
                  raise ValueError(f"Unknown format: {fmt}")
              
              print(f"✅ Script generated")
              print(f"   Format: {fmt}")
              print(f"   Length: {len(script.get('script', ''))} chars")
          
          except Exception as e:
              print(f"❌ Script generation failed: {e}")
              import traceback
              traceback.print_exc()
              sys.exit(1)
          
          # ========== PART 2: TTS SYNTHESIS =========="
          print(f"\n🎙️ PART 2: TTS Synthesis")
          print(f"-" * 70)
          
          try:
              from core.generators import tts_generator
              
              api_key = os.getenv('GOOGLE_AI_API_KEY')
              if not api_key:
                  raise ValueError("GOOGLE_AI_API_KEY not set!")
              
              audio_map = tts_generator.synthesize(
                  script=script,
                  mode=fmt,
                  api_key=api_key
              )
              
              print(f"✅ Audio synthesized")
              print(f"   Duration: {audio_map.get('total_duration_sec', 0):.1f}s")
              print(f"   Blocks: {len(audio_map.get('blocks', {}))}")
          
          except Exception as e:
              print(f"❌ TTS synthesis failed: {e}")
              import traceback
              traceback.print_exc()
              sys.exit(1)
          
          # ========== PART 3: VIDEO RENDERING =========="
          print(f"\n🎥 PART 3: Video Rendering")
          print(f"-" * 70)
          
          try:
              from core.generators import video_renderer
              
              video_path = video_renderer.render(
                  script=script,
                  audio_map=audio_map,
                  mode=fmt
              )
              
              video_file = Path(video_path)
              if video_file.exists():
                  size_mb = video_file.stat().st_size / (1024*1024)
                  print(f"✅ Video rendered: {video_path}")
                  print(f"   Size: {size_mb:.1f} MB")
              else:
                  raise FileNotFoundError(f"Video file not found: {video_path}")
          
          except Exception as e:
              print(f"❌ Video rendering failed: {e}")
              import traceback
              traceback.print_exc()
              sys.exit(1)
          
          # ========== SUMMARY =========="
          print(f"\n" + "="*70)
          print(f"✅ SUCCESS! Video generation complete")
          print(f"="*70)
          print(f"Output: {video_path}")
          print(f"Size: {size_mb:.1f} MB")
          print(f"\nReady for publishing to YouTube, TikTok, VK, Instagram")
          
          PYTHON_SCRIPT
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: horoscope-output-${{ github.run_number }}
          path: |
            output/scripts/
            output/audio/
            output/videos/
          retention-days: 7
          if-no-files-found: warn
      
      - name: Summary
        if: success()
        run: |
          echo "✅ Video generation complete"
          echo "Format: ${{ steps.params.outputs.format }}"
          echo "Date: ${{ steps.params.outputs.date }}"
          echo ""
          echo "📥 Download from artifacts"
          echo "📤 Then publish to social media"

  notify-result:
    runs-on: ubuntu-22.04
    needs: [generate-video]
    if: always()
    
    steps:
      - name: Send Telegram notification
        if: secrets.TELEGRAM_BOT_TOKEN
        run: |
          FORMAT="${{ github.event.inputs.format || 'shorts' }}"
          STATUS="${{ needs.generate-video.result }}"
          
          if [ "$STATUS" = "success" ]; then
            EMOJI="✅"
            TEXT="Horoscope video generated successfully"
          else
            EMOJI="❌"
            TEXT="Horoscope video generation failed"
          fi
          
          MESSAGE="${EMOJI} ${TEXT}%0AFormat: ${FORMAT}%0AWorkflow: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          curl -s -X POST \
            -H 'Content-Type: application/x-www-form-urlencoded' \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=$MESSAGE&parse_mode=HTML" \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" > /dev/null || true
        continue-on-error: true
