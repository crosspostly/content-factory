
name: Render Video (Robust)

on:
  workflow_dispatch:
    inputs:
      job_branch:
        description: 'Branch containing source_materials.zip'
        required: true
        type: string

permissions:
  contents: write
  actions: write
  issues: write

jobs:
  render:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Job Branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.job_branch }}
          fetch-depth: 0

      # SYSTEM DEPENDENCIES
      - name: Install System Dependencies (FFmpeg)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg libjpeg-dev zlib1g-dev

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      # INSTALL REQUESTS (Compatibility for old project ZIPs)
      - name: Install Python Libraries
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests Pillow piexif
          
      - name: Debug Environment
        run: |
          python --version
          python -m pip list

      - name: Unpack Materials
        run: |
          echo "📂 Current Directory:"
          ls -lh
          if [ -f source_materials.zip ]; then
            unzip -o source_materials.zip
            echo "✅ Unzipped successfully"
          else
            echo "❌ source_materials.zip not found!"
            exit 1
          fi

      - name: Run Assembler Script
        run: |
          echo "🎬 Starting assemble_video.py..."
          python assemble_video.py

      - name: Verify Output
        run: |
          if ls *.mp4 1> /dev/null 2>&1; then
            echo "✅ Video generated successfully"
          else
            echo "❌ No MP4 files found. Check python output above."
            exit 1
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: final-video-${{ github.run_id }}
          path: "*.mp4"
          retention-days: 7
