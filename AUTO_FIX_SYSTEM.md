# 🤖 Auto-Fix System - Automatic Error Resolution

Полностью автоматическая система исправления ошибок тестов с 2 режимами работы.

## 📋 Обзор

Auto-Fix System анализирует падающие тесты и либо **автоматически исправляет** (SIMPLE), либо создает **детальное ТЗ** для разработчика (COMPLEX).

## 🚀 Как это работает

### 1. ТЫ ПУШИШЬ КОД
```bash
git push origin feature/your-feature
```

### 2. GITHUB ACTIONS ЗАПУСКАЮТСЯ
- Workflow `tests.yml` выполняет pytest
- Если тесты ✅ - готово!
- Если тесты ❌ - запускается Auto-Fix Agent

### 3. AUTO-FIX AGENT АНАЛИЗИРУЕТ
- Скачивает логи ошибок (`pytest.log`)
- Отправляет в Qwen/Gemini для анализа
- Определяет тип ошибки и сложность
- Создает GitHub Issue с анализом

### 4A. ПРОСТАЯ ОШИБКА (AUTO-FIX) 🤖

**Типы ошибок которые исправляются автоматически:**
- ❌ Missing imports → ✅ Добавить import
- ❌ ModuleNotFoundError → ✅ Добавить в requirements.txt
- ❌ Syntax errors → ✅ Запустить black/flake8
- ❌ Missing attributes → ✅ Добавить метод в класс
- ❌ Type errors (очевидные) → ✅ Привести тип
- ❌ File not found → ✅ Создать файл
- ❌ Permission denied → ✅ Добавить chmod
- ❌ YAML syntax → ✅ Исправить синтаксис

**Что происходит:**
1. Issue получает метку `auto-fix-ready`
2. Workflow `auto-fix.yml` запускается
3. Генерирует исправление через LLM
4. Создает ветку `auto-fix-issue-{N}`
5. Применяет исправление к файлу
6. Коммитит: `🤖 auto-fix: issue #{N}`
7. Создает Pull Request
8. Тесты перезапускаются
9. Если ✅ → **auto-merge в main** 🎉

### 4B. СЛОЖНАЯ ОШИБКА (MANUAL) 👤

**Что НЕЛЬЗЯ исправить автоматически:**
- 🧠 Logic bugs (нужна бизнес-логика)
- 🏗️ Architecture issues
- 🌐 API integration problems
- ⚡ Performance issues
- ⚙️ Configuration problems

**Что происходит:**
1. Issue получает метку `needs-dev-task`
2. Workflow `create-task.yml` запускается
3. Добавляет детальное ТЗ в комментарий
4. Добавляет метки `help wanted`, `good first issue`
5. **ТЫ** читаешь ТЗ
6. **ТЫ** исправляешь код
7. **ТЫ** создаешь PR
8. Тесты проверяют
9. Если ✅ → мёржишь в main

## 📊 Decision Tree

```
Ошибка в тесте
    ↓
Workflow падает → Auto-Fix Agent запускается
    ↓
Qwen/Gemini анализирует логи
    ↓
Определяет: SIMPLE или COMPLEX?
    ↓
┌─────────────────────┬─────────────────────┐
│      SIMPLE         │      COMPLEX        │
│   (AUTO-FIX) 🤖     │    (MANUAL) 👤      │
├─────────────────────┼─────────────────────┤
│ 1. auto-fix.yml     │ 1. create-task.yml  │
│ 2. Генерирует fix   │ 2. Создает ТЗ       │
│ 3. Создает branch   │ 3. Добавляет labels │
│ 4. Коммитит         │ 4. Пингует devs     │
│ 5. Создает PR       │ 5. Ждет manual PR   │
│ 6. Тесты ✅         │ 6. Developer fixes  │
│ 7. Auto-merge ✅    │ 7. Manual merge     │
└─────────────────────┴─────────────────────┘
```

## 🔑 Workflows

### 1. `tests.yml` (Обновлен)
- Запускает тесты
- Сохраняет `pytest.log` в artifact
- При падении триггерит `auto-fix-agent.yml`

### 2. `auto-fix-agent.yml` (Обновлен)
- Анализирует ошибки через Qwen/Gemini
- Создает GitHub Issue с детальным анализом
- Определяет SIMPLE vs COMPLEX
- Добавляет соответствующие метки

### 3. `auto-fix.yml` (НОВЫЙ) 🤖
- Слушает Issues с меткой `auto-fix-ready`
- Генерирует код-fix через LLM
- Создает ветку и PR
- Применяет исправление автоматически

### 4. `create-task.yml` (НОВЫЙ) 👤
- Слушает Issues с меткой `needs-dev-task`
- Добавляет детальное ТЗ в комментарий
- Помечает как `help wanted`, `good first issue`

### 5. `auto-merge.yml` (НОВЫЙ) ✅
- Слушает успешные runs на ветках `auto-fix-*`
- Проверяет что PR от Auto-Fix Agent
- Автоматически мёржит если тесты прошли

## 🔧 Конфигурация

### Required Secrets (GitHub Settings)

```
GOOGLE_AI_API_KEY      # Для Gemini API
OPENROUTER_API_KEY     # Для Qwen через OpenRouter (опционально)
GITHUB_TOKEN           # Автоматически создается
```

### Required Permissions (Workflow)

```yaml
permissions:
  contents: write        # Для создания веток и коммитов
  issues: write          # Для создания Issues
  pull-requests: write   # Для создания PRs
  checks: read          # Для чтения статуса тестов
```

## 📝 Примеры

### Пример 1: Missing Import (AUTO-FIX)

**Ошибка:**
```
ImportError: cannot import name 'ProjectConfig' from 'core.utils.config_loader'
```

**Auto-Fix делает:**
1. Анализирует: "Missing import statement"
2. Метка: `auto-fix-ready`
3. Генерирует fix: добавляет `from core.utils.config_loader import ProjectConfig`
4. Создает PR
5. Тесты ✅
6. Auto-merge → main

**Время:** ~3-5 минут от ошибки до исправления в main

### Пример 2: Logic Bug (MANUAL)

**Ошибка:**
```
AssertionError: Expected result=100 but got 50
```

**Auto-Fix делает:**
1. Анализирует: "Logic error in calculation, business logic needed"
2. Метка: `needs-dev-task`
3. Создает ТЗ:
   ```
   ## Проблема
   Функция calculate_score() возвращает неправильный результат
   
   ## Что нужно сделать
   1. Открыть core/utils/scoring.py
   2. Проверить формулу расчета
   3. Ожидаем: score = base * 2
   4. Сейчас: score = base
   5. Исправить формулу
   6. Запустить pytest tests/test_scoring.py
   ```
4. Ты исправляешь код
5. Создаешь PR
6. Мёржишь после review

## 🎯 Метрики и KPI

### Целевые показатели:
- **Auto-Fix Rate:** 60-70% ошибок исправляются автоматически
- **Time to Fix (SIMPLE):** < 5 минут
- **Time to Fix (COMPLEX):** < 1 час (с учетом dev работы)
- **False Positive Rate:** < 5% (неправильные auto-fix)

### Мониторинг:
- GitHub Issues с метками `auto-fix-ready` и `needs-dev-task`
- Успешность PRs от auto-fix
- Время от ошибки до merge

## 🚫 Что НЕ делает Auto-Fix

1. ❌ Не мёржит в `main` напрямую (только через PR)
2. ❌ Не исправляет без создания Issue (для прозрачности)
3. ❌ Не запускается на `main` (только на feature branches)
4. ❌ Не делает auto-merge если тесты падают
5. ❌ Не удаляет код (только добавляет/изменяет)

## 🔍 Troubleshooting

### Auto-Fix не запустился

**Проблема:** Тесты упали, но Issue не создался

**Решения:**
1. Проверь что ветка не `main`
2. Проверь что workflow реально упал (не `continue-on-error: true`)
3. Проверь логи `auto-fix-agent.yml`
4. Проверь что `pytest.log` есть в artifacts

### Auto-Fix создал неправильный PR

**Решения:**
1. Закрой PR (не мёржь!)
2. Удали метку `auto-fix-ready` из Issue
3. Добавь метку `needs-dev-task`
4. Исправь вручную
5. Оставь комментарий в Issue что пошло не так (для улучшения системы)

### Тесты все равно падают после auto-fix

**Это нормально!** Auto-Fix не идеален. Если тесты падают:
1. PR не будет auto-merged
2. Issue остается открытым
3. Ты видишь PR и можешь доработать
4. Или закрываешь PR и делаешь manual fix

## 📚 См. также

- [AUTO_FIX_AGENT_TESTS_README.md](./AUTO_FIX_AGENT_TESTS_README.md) - Тесты системы
- [AUTO_FIX_AGENT_TEST_SCENARIOS.md](./AUTO_FIX_AGENT_TEST_SCENARIOS.md) - Тестовые сценарии
- [WORKFLOWS.md](./WORKFLOWS.md) - Документация по всем workflows
- [core/auto_fix_agent.py](./core/auto_fix_agent.py) - Исходный код агента

## 🎉 Результат

**main НИКОГДА не ломается!** ✅

Все ошибки либо исправляются автоматически, либо превращаются в четкие задачи для разработчиков.
